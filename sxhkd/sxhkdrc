super + semicolon
  alacritty

super + Escape
  pkill -USR1 -x sxhkd

# bspwm
super + f
  bspc node -t ~fullscreen

super + {_,shift + }{j,k}
  bspc node -{f,s} {next,prev}.local

super + {l,grave}
  bspc desktop -f last

super + {_,shift + }{1-5}
  bspc {desktop -f,node -d} '^{1-5}'
